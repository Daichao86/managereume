# Nginx 反向代理配置示例
# 文件路径: /etc/nginx/sites-available/resume-talent-mgr
# 启用: ln -s /etc/nginx/sites-available/resume-talent-mgr /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name your-domain.com;   # 改为你的域名或服务器IP

    # 上传文件大小限制（简历文件通常不超过10MB）
    client_max_body_size 10M;

    # 反向代理到 Node.js 服务
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }

    # 开启 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
    gzip_min_length 1024;
}

# HTTPS 配置（使用 certbot 申请证书后自动生成，或手动配置）
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     client_max_body_size 10M;
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
